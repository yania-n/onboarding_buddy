# ============================================================
# OnboardingBuddy -- Python Dependencies
# ============================================================

# -- Core AI --------------------------------------------------
anthropic>=0.40.0

# -- Embeddings & Vector Search -------------------------------
voyageai>=0.2.3
faiss-cpu>=1.7.4

# -- UI Framework ---------------------------------------------
# Pin gradio to match HF Spaces sdk_version ("4.44.0" in README.md).
# Newer majors (6.x) remove kwargs like Chatbot(type=...) and break the app
# locally; older point releases are fine. The pin matches what HF runs.
gradio==4.44.0
gradio_client==1.3.0
#
# Python 3.13 compatibility patches for Gradio 4.44:
#   audioop-lts   -> restores audioop stdlib module removed in Python 3.13
#   huggingface_hub pin -> keeps HfFolder available (removed in hf_hub >=0.26)
audioop-lts>=0.2.1
huggingface_hub>=0.19.0,<0.26.0
#
# Starlette pin: Starlette 0.29+ changed TemplateResponse signature in a way
# that breaks Gradio 4.44's routes.py (passes template name where request is
# now expected). Pinning to <0.29 restores the old positional API.
starlette>=0.27.0,<0.29.0

# -- Google Drive Integration ---------------------------------
google-api-python-client>=2.100.0
google-auth>=2.23.0
gdown>=5.1.0

# -- Document Processing --------------------------------------
pdfplumber>=0.10.3
python-docx>=1.1.0

# -- Data & Scheduling ----------------------------------------
pydantic>=2.0.0
python-dotenv>=1.0.0
apscheduler>=3.10.0

# -- HTTP & Networking ----------------------------------------
requests>=2.31.0
tenacity>=8.2.0

# -- Utilities ------------------------------------------------
numpy>=1.26.0
rich>=13.0.0
