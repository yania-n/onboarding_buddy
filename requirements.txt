# ============================================================
# OnboardingBuddy -- Python Dependencies
# ============================================================

# -- Core AI --------------------------------------------------
anthropic>=0.40.0

# -- Embeddings & Vector Search -------------------------------
voyageai>=0.2.3
faiss-cpu>=1.7.4

# -- UI Framework ---------------------------------------------
# Gradio is pre-installed by HF Spaces (sdk_version in README.md).
# Do NOT re-pin gradio here -- it conflicts with the Space SDK.
# For local dev: pip install gradio==6.12.0

# -- Google Drive Integration ---------------------------------
google-api-python-client>=2.100.0
google-auth>=2.23.0
gdown>=5.1.0

# -- Document Processing --------------------------------------
pdfplumber>=0.10.3
python-docx>=1.1.0

# -- Data & Scheduling ----------------------------------------
pydantic>=2.0.0
python-dotenv>=1.0.0
apscheduler>=3.10.0

# -- HTTP & Networking ----------------------------------------
requests>=2.31.0
tenacity>=8.2.0

# -- Utilities ------------------------------------------------
numpy>=1.26.0
rich>=13.0.0

# -- Python 3.13 compatibility --------------------------------
# audioop was removed in Python 3.13; audioop-lts restores it for 3.12+.
audioop-lts>=0.2.1
